name: prophet_kacou
description: "This app contains all the message of the Prophet Kacou Philippe."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3
  flutter_riverpod: ^3.0.3
  url_launcher: ^6.3.2
  provider: ^6.1.5+1
  sqflite: ^2.4.0
  path: ^1.9.0
  path_provider: ^2.1.5
  package_info_plus: ^8.0.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  flutter_html: ^3.0.0-beta.2
  photo_view: ^0.15.0
  youtube_player_flutter: ^9.1.3
  intl: ^0.18.1
  just_audio: ^0.10.5
  dio: ^5.7.0
  # Flutter Rust Bridge pour la communication Rust-Flutter
  flutter_rust_bridge: ^2.0.0
  # Permissions pour Android/iOS
  permission_handler: ^11.0.0
  slugify: ^2.0.0
  share_plus: ^10.0.0
  pdf: ^3.10.0
  printing: ^5.11.0
  convert: ^3.1.1
  open_filex: ^4.4.0
  flutter_inappwebview: ^6.0.0
  flutter_native_splash: ^2.3.10

  # Utilitaires
  async: ^2.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  rename: ^3.0.2

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  build_runner: ^2.4.6
  freezed: ^2.4.1
  json_serializable: ^6.8.0
  # Générateur de code pour flutter_rust_bridge
  ffigen: ^9.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# Configuration du Splash Screen natif
flutter_native_splash:
  # Couleur de fond du splash screen
  color: "#FFFFFF"
  # Image au centre (votre icône)
  # Taille recommandée: 1152x1152 pixels (3x de 384x384)
  # ou minimum 512x512 pixels
  image: assets/icons/icon512x512.png

  # Configuration Android (versions < 12)
  android: true

  # Configuration Android 12+ (obligatoire depuis Android 12)
  android_12:
    # Image pour Android 12+ (doit être plus petite)
    # Taille recommandée: 288x288 pixels (zone de sécurité)
    # Maximum: 432x432 pixels
    image: assets/icons/icon512x512.png

    # Couleur de fond pour Android 12+
    color: "#FFFFFF"
    # Configuration iOS
  ios: true
    # Image pour iOS (optionnel, si différente)
    # Taille recommandée: 1152x1152 pixels
  image_ios: assets/icons/icon512x512.png

  # Couleur de fond iOS (si différente d'Android)
  # color_ios: "#FFFFFF"

  # Configuration Web (optionnel)
  web: false # Supprimer le splash screen Flutter par défaut

  # Mode plein écran
  fullscreen: true

  # Supprime la barre de navigation Android
  android_gravity: center
  ios_content_mode: center
  # Configuration du nom de l'application
  #flutter_launcher_name:
  #name: "Prophet Kacou" # C'est ICI que vous mettez le nom avec majuscules et espaces

  # Configuration de l'icône de l'application
flutter_launcher_icons:
  android: true
  ios: true

  # Image principale (taille recommandée: 1024x1024 pixels minimum)
  image_path: "assets/icons/icon512x512.png"

  # Configuration Android - Icône adaptative
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icons/icon512x512.png"

  # Configuration iOS
  remove_alpha_ios: true
  # Tailles personnalisées (optionnel)
  # min_sdk_android: 21

  # Image pour le mode sombre (optionnel)
  image_path_android: "assets/icons/icon512x512.png"
  image_path_ios: "assets/icons/icon512x512.png"
  # The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

  assets:
  - assets/locales/
  - assets/images/
  - assets/images/drapeau/
  - assets/fonts/
  - assets/icons/
  - assets/databases/common/common.db
  - assets/databases/en/matth25v6_en.db
  - assets/databases/fr/matth25v6_fr.db
  - assets/databases/pt/matth25v6_pt.db
  - assets/databases/es/matth25v6_es.db
  - assets/databases/sa/matth25v6_ar.db
  - assets/databases/ir/matth25v6_fa.db
  - assets/databases/cn/matth25v6_zh.db
  - assets/databases/in/matth25v6_hi.db
